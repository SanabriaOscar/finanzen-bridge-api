# Carpeta de entregables (build local)

Generado por:

```powershell
mvn clean package -Pwith-ui,win-exe
```

| Archivo | Descripción |
|---------|-------------|
| `finanzen-bridge-1.0.0.exe` | Instalador Windows (front + back + runtime Java) |

No commitear binarios pesados salvo releases taggeadas.
